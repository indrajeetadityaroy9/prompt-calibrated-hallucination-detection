# AG-SAR v0.4.0 Reproducibility Requirements
# Generated for paper experiment reproducibility with H100 optimizations

# --- Core Deep Learning (H100 Optimized) ---
torch>=2.2.0
transformers>=4.40.0,<4.45.0  # CRITICAL: >=4.45 breaks attention hooks
accelerate>=0.26.0
numpy>=1.20.0

# --- Hardware Acceleration (Linux/H100 specific) ---
# Note: flash-attn often requires manual build if wheels aren't found
triton>=2.1.0; sys_platform == "linux"
flash-attn>=2.5.0; sys_platform == "linux"

# --- Evaluation Framework ---
datasets>=2.14.0
scikit-learn>=1.3.0
scipy>=1.10.0
pydantic>=2.0.0
pyyaml>=6.0
tqdm>=4.66.0
pandas>=2.0.0

# --- Baselines (Competitors) ---
sentence-transformers>=2.2.2
nltk>=3.8

# --- Visualization ---
matplotlib>=3.7.0
seaborn>=0.12.0

# --- Development & Testing ---
pytest>=7.4.0
pytest-cov>=4.0
black>=23.0.0
ruff>=0.1.0
mypy>=1.0
